import {NextRequest} from "next/server";import {beforeEach,expect,it,vi} from "vitest";
const save=vi.hoisted(()=>vi.fn().mockResolvedValue("ok"));vi.mock("@/lib/supabase/route-handler",()=>({createRouteHandlerClient:()=>({applyAuthState:(response:Response)=>response,supabase:{}})}));vi.mock("@/lib/auth/verified-user",()=>({getVerifiedUserId:async()=>"coach"}));vi.mock("@/lib/auth/team-context",()=>({loadTeamContext:async()=>({teamId:"team",seasonId:"season",seasonName:"S"})}));vi.mock("@/lib/supabase/admin",()=>({createAdminClient:()=>({kind:"admin"})}));vi.mock("@/features/trainings/training-plans",async original=>({...await original<typeof import("@/features/trainings/training-plans")>(),saveTrainingPlan:save}));import {POST} from "./route";
const request=()=>new NextRequest("https://app.example/trainings/t1/save",{method:"POST",body:new URLSearchParams({revision:"1",focus:"Passning",key_message:"PASSA",coach_notes:"Guide",status:"planned",items:'[{"section":"technique","title":"Passa","coachingPoints":[]}]'})});beforeEach(()=>save.mockReset().mockResolvedValue("ok"));it("saves validated plan through server boundary",async()=>{const response=await POST(request(),{params:Promise.resolve({id:"t1"})});expect(save).toHaveBeenCalledWith({kind:"admin"},expect.objectContaining({actorUserId:"coach",trainingId:"t1",revision:1,status:"planned"}));expect(response.headers.get("location")).toBe("https://app.example/trainings/t1?change=saved")});it("maps stale saves without overwriting",async()=>{save.mockResolvedValue("stale");const response=await POST(request(),{params:Promise.resolve({id:"t1"})});expect(response.headers.get("location")).toContain("change=stale")});
