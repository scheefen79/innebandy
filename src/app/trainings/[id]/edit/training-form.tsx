"use client";
/* eslint-disable @typescript-eslint/no-unused-vars -- id tas avsiktligt bort ur mutationspayloaden */
import {useState} from "react";import type {TrainingItem,TrainingPlan,TrainingSection} from "@/features/trainings/training-plans";
const sectionText:Record<TrainingSection,string>={gathering:"Samling",warmup:"Uppvärmning",technique:"Teknik",match_exercise:"Matchövning",closing:"Avslutning"};
export function TrainingForm({training}:{training:TrainingPlan}){const [items,setItems]=useState(training.items);const update=(index:number,patch:Partial<TrainingItem>)=>setItems(current=>current.map((item,i)=>i===index?{...item,...patch}:item));const move=(index:number,direction:-1|1)=>{const target=index+direction;if(target<0||target>=items.length)return;setItems(current=>{const next=[...current];[next[index],next[target]]=[next[target],next[index]];return next})};return <form action={`/trainings/${training.id}/save`} method="post" className="mt-6 space-y-6"><input type="hidden" name="revision" value={training.revision}/><input type="hidden" name="items" value={JSON.stringify(items.map(({id,...item})=>({...item,coachingPoints:item.coachingPoints})))}/><label className="block"><span className="font-semibold">Fokus</span><input name="focus" required maxLength={500} defaultValue={training.focus} className="mt-2 min-h-12 w-full rounded-xl border border-slate-300 px-3"/></label><label className="block"><span className="font-semibold">Huvudbudskap</span><input name="key_message" required maxLength={300} defaultValue={training.keyMessage} className="mt-2 min-h-12 w-full rounded-xl border border-slate-300 px-3"/></label><label className="block"><span className="font-semibold">Gemensamma tränaranteckningar</span><textarea name="coach_notes" maxLength={5000} defaultValue={training.coachNotes??""} rows={4} className="mt-2 w-full rounded-xl border border-slate-300 p-3"/></label><fieldset><legend className="text-xl font-semibold">Moment</legend><p className="mt-1 text-sm text-slate-600">Minuterna är endast en guide.</p><div className="mt-4 space-y-4">{items.map((item,index)=><section key={item.id} className="rounded-2xl border border-slate-200 bg-white p-4"><div className="flex items-center justify-between gap-2"><span className="text-xs font-semibold uppercase text-blue-700">{sectionText[item.section]}</span><div><button type="button" onClick={()=>move(index,-1)} aria-label={`Flytta ${item.title} upp`} className="min-h-11 px-3">↑</button><button type="button" onClick={()=>move(index,1)} aria-label={`Flytta ${item.title} ned`} className="min-h-11 px-3">↓</button><button type="button" onClick={()=>setItems(current=>current.filter((_,i)=>i!==index))} className="min-h-11 px-3 text-red-700">Ta bort</button></div></div><label className="mt-2 block text-sm font-semibold">Rubrik<input value={item.title} onChange={event=>update(index,{title:event.target.value})} className="mt-1 min-h-11 w-full rounded-lg border border-slate-300 px-3"/></label><label className="mt-3 block text-sm font-semibold">Cirka minuter<input type="number" min={1} max={120} value={item.guideMinutes??""} onChange={event=>update(index,{guideMinutes:event.target.value?Number(event.target.value):null})} className="mt-1 min-h-11 w-28 rounded-lg border border-slate-300 px-3"/></label><label className="mt-3 block text-sm font-semibold">Syfte<textarea value={item.purpose??""} onChange={event=>update(index,{purpose:event.target.value||null})} rows={2} className="mt-1 w-full rounded-lg border border-slate-300 p-3"/></label><label className="mt-3 block text-sm font-semibold">Genomförande<textarea value={item.instructions??""} onChange={event=>update(index,{instructions:event.target.value||null})} rows={3} className="mt-1 w-full rounded-lg border border-slate-300 p-3"/></label><label className="mt-3 block text-sm font-semibold">Coachingpunkter, en per rad<textarea value={item.coachingPoints.join("\n")} onChange={event=>update(index,{coachingPoints:event.target.value.split("\n").map(value=>value.trim()).filter(Boolean)})} rows={3} className="mt-1 w-full rounded-lg border border-slate-300 p-3"/></label><label className="mt-3 block text-sm font-semibold">Källänk<input type="url" value={item.sourceUrl??""} onChange={event=>update(index,{sourceUrl:event.target.value||null})} className="mt-1 min-h-11 w-full rounded-lg border border-slate-300 px-3"/></label></section>)}</div><button type="button" onClick={()=>setItems(current=>[...current,{id:crypto.randomUUID(),section:"match_exercise",position:current.length+1,title:"Ny övning",guideMinutes:null,purpose:null,instructions:null,coachingPoints:[],sourceUrl:null,sourceImageUrl:null}])} className="mt-4 min-h-11 rounded-xl border border-blue-700 px-4 font-semibold text-blue-700">Lägg till övning</button></fieldset><label className="block"><span className="font-semibold">Status</span><select name="status" defaultValue={training.status} className="mt-2 min-h-12 w-full rounded-xl border border-slate-300 px-3"><option value="draft">Ej planerad</option><option value="planned">Planerad</option>{training.status==="planned"?<option value="completed">Genomförd</option>:null}</select></label><button className="min-h-12 w-full rounded-xl bg-blue-700 px-4 font-semibold text-white">Spara träningsplan</button></form>}
