/**
 * Lagets standardtrupp: antal ordinarie spelare som kallas till en match.
 * Värdet är ett förval i formuläret för ny match, inte en spärr. En coach kan
 * fortfarande ange ett annat antal för en enskild match.
 */
export const DEFAULT_TARGET_PLAYERS = 10;
